/*
    Las tuplas son arreglos pero con n cantidad de elementos y cada elemento con un tipo de dato especifico.
*/

// Ejemplo #1
// En este ejemplo la tupla acepta dos elementos obligatorios y con un tipo de dato especifico cada uno, si pasamos un solo elemento o los pasamos invertidos el compilador nos lanzara un error.
let tuple: [string, number] = ['Daniel', 22];

// Ejemplo #2
let tuple2: [Object, boolean] = [{}, true];